import { View } from 'react-native';

// This is a placeholder screen for the navigation structure
// The actual add functionality is handled by the floating + button
export default function AddPlaceholder() {
  return <View />;
}